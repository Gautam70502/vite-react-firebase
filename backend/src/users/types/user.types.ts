export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}

export enum MemberShipStatus {
  ACTIVE = 'active',
  DEACTIVE = 'deactive',
}

export enum contactType {
  HOME = 'home',
  MOBILE = 'mobile',
  PHONE = 'phone',
}

export enum addressType {
  ADDRESS = 'address',
  BILLING = 'billing',
}
